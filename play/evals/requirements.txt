# phase 1
numpy
pytest
scikit-learn

# agent_sft phase 1: nudge_fire_rate task parses agent_engine scenario YAML frontmatter
pyyaml

# agent_sft phase 1: aggregate_seeds.py report generation (DECISIONS §8 explicit trade-off:
# accept pandas import startup tax for groupby/mean/std + json_normalize + tabulate line compression)
pandas
tabulate

# phase 2 lexical metrics
sacrebleu
rouge_score
nltk

# phase 2 embedding metric (BERTScore)
# torch wheel is large (~200MB); mBERT/bert-base-chinese ~400MB download on first use
torch
transformers
bert-score
certifi

# phase 4 RAG IR metrics (ranx: numba JIT Python wrapper over trec_eval)
# pulls heavy deps (numba / pyarrow / fastparquet etc.) but avoids hand-rolling IR formulas
ranx

# phase 8 IAA multi-rater agreement (family 1 second half)
# - statsmodels: fleiss_kappa + aggregate_raters (multi-rater nominal κ)
# - krippendorff: alpha (nominal/ordinal/interval level)
# no irrCAC (gwet hand-computed) / pingouin (ICC(1,1) hand-computed + ICC(2,1)/(3,1) deferred) /
# audtorch (Lin's CCC 5-line hand compute avoids full torch stack) — DECISIONS §8 explicitly recorded
statsmodels>=0.14
krippendorff>=0.6

# phase 4 RAG subprocess deps (needed by rag/query.py passthrough; rag/requirements.txt is
# source of truth for standalone rag usage; DECISIONS §8.3 wave 4 dual-manifest sync concession — bump both when deps drift)
chromadb
rank-bm25
tokenizers
sentence-transformers
