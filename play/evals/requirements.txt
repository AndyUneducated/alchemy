# phase 1
numpy
pytest
scikit-learn

# phase 2 lexical metrics
sacrebleu
rouge_score
nltk

# phase 2 embedding metric (BERTScore)
# torch wheel 较大 (~200MB)；mBERT/bert-base-chinese 首次 ~400MB 下载
torch
transformers
bert-score
certifi

# phase 4 RAG IR metrics (ranx：trec_eval 的 numba JIT Python 包装)
# 拉一系列重依赖（numba / pyarrow / fastparquet 等），但避免自实现 IR 公式
ranx
