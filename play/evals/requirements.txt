# phase 1
numpy
pytest
scikit-learn

# phase 2 lexical metrics
sacrebleu
rouge_score
nltk

# phase 2 embedding metric (BERTScore)
# torch wheel 较大 (~200MB)；mBERT/bert-base-chinese 首次 ~400MB 下载
torch
transformers
bert-score
certifi

# phase 4 RAG IR metrics (ranx：trec_eval 的 numba JIT Python 包装)
# 拉一系列重依赖（numba / pyarrow / fastparquet 等），但避免自实现 IR 公式
ranx

# phase 8 IAA multi-rater agreement (族 1 后半)
# - statsmodels: fleiss_kappa + aggregate_raters（多 rater nominal κ）
# - krippendorff: alpha (nominal/ordinal/interval level)
# 不引 irrCAC（gwet 手算）/ pingouin（ICC(1,1) 手算 + ICC(2,1)/(3,1) deferred）/
# audtorch（Lin's CCC 手算 5 行避 torch 全套）—— DECISIONS §8 显式登记
statsmodels>=0.14
krippendorff>=0.6
