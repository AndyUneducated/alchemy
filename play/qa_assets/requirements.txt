# Python dependencies for qa_assets (beyond agent_engine / workflow / rag).
# - jinja2: render_md hook uses Jinja2 markdown template rendering
# - pyyaml: to_yaml hook serialization (workflow also uses it; declared here explicitly)
