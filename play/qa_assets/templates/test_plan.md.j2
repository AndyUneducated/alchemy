{# Test plan markdown template (Jinja2). Rendered by hooks/render_md.py.
   Inputs:
     sections      — artifact dict from agent stage (dict[str, str])
     requirements  — list[dict] from load_each_prd (req_id / title / assignee /
                     sprint_start / sprint_end / priority / description / prd_md)

   Design: template does **not** rewrite section content (specialists already wrote
   markdown); only adds a scope & schedule metadata table at top (sprint / assignee
   as columns, plan §8 P5), then pastes artifact sections in order. Critic Feedback
   goes in an appendix at the end.
#}
# Test plan — {{ requirements | length }} requirements

## Scope & schedule

| req_id | title | assignee | priority | sprint |
|---|---|---|---|---|
{% for r in requirements -%}
| {{ r.req_id }} | {{ r.title }} | {{ r.assignee }} | {{ r.priority or '?' }} | {{ r.sprint_start }} ~ {{ r.sprint_end }} |
{% endfor %}

## Atomic Requirements

{{ sections.get('Atomic Requirements', '_(decomposer did not produce)_') }}

## Risk Levels

{{ sections.get('Risk Levels', '_(risk_grader did not produce)_') }}

## Test Cases

{{ sections.get('Test Cases', '_(case_generator did not produce)_') }}

## Non-functional

{{ sections.get('Non-functional', '_(nfr_planner did not produce)_') }}

---

## Appendix: Critic Feedback

{{ sections.get('Critic Feedback', '_(critic did not produce)_') }}
